ktor {
     development = true
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ classes ]
    }

    application {
        modules = [ org.hyperskill.blackboard.ApplicationKt.module ]
    }
}
